import sys
import os

# sys.path loads the content of the PYTHONPATH environment variables, which contains a list of directories
# in which Python looks for modules
sys.path.append('your_drive/your_directory')

print(sys.path)
